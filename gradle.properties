org.gradle.jvmargs=-Xmx8G
org.gradle.caching=false
org.gradle.configuration-cache=false

# Plugin metadata shared by every mod project in this build. What differs per plugin -- the
# entrypoint and the description -- is set in that project's own build.gradle.kts, because the
# manifest `Name` is `project.name` and a pack is identified by `Group:Name`.
plugin_group=Technic
plugin_author=Raphael Dohnalek
plugin_website=
server_version=^0.6.4
hytale.decompile_partial=true
